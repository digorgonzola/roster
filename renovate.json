{
  "$schema": "https://docs.renovatebot.com/renovate-schema.json",
  "extends": [
    "config:best-practices",
    ":pinAllExceptPeerDependencies",
    "schedule:weekly"
  ],
  "timezone": "Australia/Hobart",
  "platformAutomerge": false,
  "automergeStrategy": "squash",
  "labels": ["dependencies"],
  "minimumReleaseAge": "14 days",
  "internalChecksFilter": "strict",
  "semanticCommits": "enabled",
  "semanticCommitType": "build",
  "lockFileMaintenance": {
    "enabled": true,
    "automerge": true,
    "schedule": ["before 6am on the first day of the month"]
  },
  "postUpdateOptions": ["pnpmDedupe"],
  "osvVulnerabilityAlerts": true,
  "customManagers": [
    {
      "customType": "regex",
      "description": "Bump the pinned zizmor CLI version in both places it is pinned: the `version: X.Y.Z # pin the CLI` input to zizmor-action, and ZIZMOR_VERSION in the git pre-commit hook. They must move together or the local and CI gates enforce different rulesets. Copied from the isw-kudos/renovate-config :gha preset, which this repo cannot extend: the hosted Renovate app cannot read that private repo.",
      "managerFilePatterns": [
        "/^\\.github/workflows/zizmor\\.ya?ml$/",
        "/^\\.githooks/pre-commit$/"
      ],
      "matchStrings": [
        "version:\\s*v?(?<currentValue>\\d+\\.\\d+\\.\\d+)\\s*#\\s*pin the CLI",
        "ZIZMOR_VERSION=(?<currentValue>\\d+\\.\\d+\\.\\d+)"
      ],
      "depNameTemplate": "zizmorcore/zizmor",
      "datasourceTemplate": "github-releases",
      "extractVersionTemplate": "^v?(?<version>.+)$"
    }
  ],
  "vulnerabilityAlerts": {
    "labels": ["security"]
  },
  "packageRules": [
    {
      "description": "Leave pnpm-workspace override ranges alone. They are hand-written security floors; pinning one rewrites the range to the latest major (see PR #26, which pinned the nanoid override to 6.0.1)",
      "matchDepTypes": ["pnpm-workspace.overrides"],
      "enabled": false
    },
    {
      "description": "GitHub Actions updates use the ci: commit type, matching repo convention",
      "matchManagers": ["github-actions"],
      "semanticCommitType": "ci"
    },
    {
      "description": "Group React runtime and type packages into one PR",
      "groupName": "react",
      "matchPackageNames": [
        "react",
        "react-dom",
        "@types/react",
        "@types/react-dom"
      ]
    },
    {
      "description": "Group Vite, Vitest, and related plugins into one PR to keep the ecosystem in lockstep",
      "groupName": "vite",
      "matchPackageNames": [
        "vite",
        "vitest",
        "/^@vitejs//",
        "/^@vitest//",
        "/^vite-/"
      ]
    },
    {
      "description": "Major updates wait for approval on the Dependency Dashboard",
      "matchUpdateTypes": ["major"],
      "dependencyDashboardApproval": true
    },
    {
      "description": "Automerge devDependency patch/minor updates once the required-checks gate passes",
      "matchDepTypes": ["devDependencies"],
      "matchUpdateTypes": ["minor", "patch"],
      "automerge": true
    },
    {
      "description": "Automerge GitHub Actions digest and patch/minor updates",
      "matchManagers": ["github-actions"],
      "matchUpdateTypes": ["minor", "patch", "digest", "pin", "pinDigest"],
      "automerge": true
    },
    {
      "description": "Zizmor: the pinned CLI version and the zizmor-action release must move together in ONE gated PR. The action ships a per-version allowlist and fails with 'Unknown version' when the CLI pin is newer than the SHA-pinned action release supports (see isw-kudos/boards#341). The workflow `version:` literal is claimed twice: the built-in github-actions manager reads it as the docker dep ghcr.io/zizmorcore/zizmor, and the zizmor customManager reads it as the github-releases dep zizmorcore/zizmor. Both names are listed so a CLI bump cannot land alone. Mirrors the isw-kudos/renovate-config default preset. This rule MUST stay after the GitHub Actions automerge rule so automerge: false wins.",
      "groupName": "Zizmor",
      "matchPackageNames": [
        "ghcr.io/zizmorcore/zizmor",
        "zizmorcore/zizmor",
        "zizmorcore/zizmor-action"
      ],
      "semanticCommitType": "ci",
      "automerge": false,
      "dependencyDashboardApproval": true
    }
  ]
}
